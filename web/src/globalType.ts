export type SelectOptions = {
  value: number;
  label: string;
}

export type AnimalModel = {
  id: number;
  name: string,
  breed: string,
  sex: string,
  weight: string | number,
  height: string | number,
  age: string | number
}